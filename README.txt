This is a transparent WCF Proxy that works similalry to Apache's mod_proxy. Designate a url to proxy in the
web config and the path of the deployed proxy service. Only works for "GET" currently. Other verbs should be easy to add though.

GPL3.0 License. 